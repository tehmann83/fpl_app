import React from 'react';
import Search from '../search/Search';
import './Header.scss';

const Header = () => {
	return (
		<div className="header">
			<Search />
		</div>
	);
};

export default Header;
